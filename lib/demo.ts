/**
 * Shared constant for the public demo jam room.
 * Created by `npm run db:seed` (see prisma/seed.ts) and surfaced in the
 * dashboard empty state so new users can peek into a live-feeling room.
 */
export const DEMO_STREAM_CODE = "MUZI-JAM";
